#pragma once

#define libsupermesh_VERSION_MAJOR @libsupermesh_VERSION_MAJOR@
#define libsupermesh_VERSION_MINOR @libsupermesh_VERSION_MINOR@
#define libsupermesh_VERSION_MINOR_CHANGE @libsupermesh_VERSION_MINOR_CHANGE@

#define PROFILE @libsupermesh_PROFILE@
#if PROFILE == 0
#undef PROFILE
#endif

#define OVERLAP_COMPUTE_COMMS @libsupermesh_OVERLAP_COMPUTE_COMMS@
#if OVERLAP_COMPUTE_COMMS == 0
#undef OVERLAP_COMPUTE_COMMS
#endif

#define HAVE_PETSC @libsupermesh_HAVE_PETSC@
#if HAVE_PETSC == 0
#undef HAVE_PETSC
#endif

#define ENABLE_JUDY @libsupermesh_ENABLE_JUDY@
#if ENABLE_JUDY == 0
#undef ENABLE_JUDY
#endif

#define DDEBUG @libsupermesh_DEBUG@
#if DDEBUG == 0
#undef DDEBUG
#define NDEBUG
#else
#undef NDEBUG
#endif
